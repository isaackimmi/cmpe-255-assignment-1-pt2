import "./styles.css";

const API = import.meta.env.VITE_API_BASE_URL || "/api";
const state = {metrics: null, importance: [], slice: null, selectedSlice: "all", population: "primary", error: null};
const app = document.querySelector("#app");

const html = (strings, ...values) => strings.reduce((out, string, index) => out + string + (values[index] ?? ""), "");
const esc = (value) => String(value ?? "").replace(/[&<>"']/g, (char) => ({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[char]));
const pct = (value) => `${(Number(value) * 100).toFixed(1)}%`;
const sec = (value) => `${Number(value).toFixed(1)} sec`;

function shell() {
  app.innerHTML = html`<header class="topbar"><a class="brand" href="#top"><span class="mark">01</span><span><b>TAXI LAB</b><small>NYC TRIP DURATION</small></span></a><nav><a href="#evidence">Evidence</a><a href="#explorer">Explorer</a><a href="#estimate">Estimator</a></nav><span id="health" class="status"><i></i> CONNECTING</span></header>
  <main id="top"><section class="hero"><div><p class="eyebrow">CMPE 255 · E2E MODEL SERVICE</p><h1>Turn trip context<br /><em>into travel time.</em></h1><p class="lede">A chronological regression experiment served as an inspectable API and an evidence-first analytical client.</p><div class="hero-actions"><a class="button primary" href="#explorer">Explore holdout <span>↘</span></a><a class="text-link" href="#estimate">Try a route →</a></div></div><div class="hero-orbit"><div class="orbit"></div><div class="orbit two"></div><div class="hero-number"><small>MODEL MAE</small><strong id="hero-mae">—</strong><span>seconds</span><b id="hero-delta">—</b></div></div></section>
  <section id="evidence" class="section"><div class="section-head"><div><p class="eyebrow">01 / evidence</p><h2>The model earns its headline.</h2></div><p>Every number below is returned by FastAPI from the checked-in experiment artifacts. The Python run remains the source of truth.</p></div><div id="metrics" class="metric-grid"><div class="loading">Loading experiment evidence…</div></div><div class="evidence-grid"><article class="panel"><div class="panel-head"><span class="eyebrow">temporal split</span><span class="chip">forward-only</span></div><div id="split" class="split-card">Reading split metadata…</div></article><article class="panel"><div class="panel-head"><span class="eyebrow">model lens</span><span class="chip">coefficients</span></div><div id="importance" class="importance">Reading feature importance…</div></article></div></section>
  <section id="explorer" class="section dark"><div class="section-head light"><div><p class="eyebrow">02 / analytical explorer</p><h2>Interrogate the holdout.</h2></div><p>Change the population or slice to recompute MAE, RMSE, and R² on the server from the prediction artifact.</p></div><div class="toolbar panel"><label>Population<select id="population"><option value="primary">All eligible holdout</option><option value="robust">Train-threshold inliers · sensitivity</option></select></label><label>Slice<select id="slice"><option value="all">All rows</option><option value="rush">Rush hour</option><option value="off_peak">Off-peak</option><option value="short">Short route</option><option value="long">Long route</option><option value="weekend">Weekend</option><option value="weekday">Weekday</option></select></label></div><div id="slice-result" class="slice-layout"><div class="loading">Loading holdout slice…</div></div></section>
  <section id="estimate" class="section estimate-section"><div class="section-head"><div><p class="eyebrow">03 / API-backed what-if</p><h2>Sketch a trip.</h2></div><p>This request is validated by FastAPI and served by the ML adapter. It deliberately mirrors the synthetic generator and is labeled as a teaching estimate.</p></div><div class="estimate-grid"><form id="estimate-form" class="panel form-grid"><label>Pickup latitude<input name="pickup_latitude" type="number" step="0.001" value="40.748" required /></label><label>Pickup longitude<input name="pickup_longitude" type="number" step="0.001" value="-73.985" required /></label><label>Drop-off latitude<input name="dropoff_latitude" type="number" step="0.001" value="40.765" required /></label><label>Drop-off longitude<input name="dropoff_longitude" type="number" step="0.001" value="-73.955" required /></label><label class="wide">Pickup time<input name="pickup_datetime" type="datetime-local" value="2016-03-18T17:30" required /></label><label>Passengers<select name="passenger_count"><option>1</option><option selected>2</option><option>3</option><option>4</option></select></label><button class="button primary wide" type="submit">Request API estimate ↗</button><p id="estimate-error" class="error wide" role="alert"></p></form><article id="estimate-result" class="estimate-result panel"><span class="eyebrow">API RESPONSE</span><strong id="estimate-time">—:—</strong><p id="estimate-summary">Submit a valid route to see the server response.</p><div class="mini-stat"><span>distance</span><b id="estimate-distance">—</b><span>context</span><b id="estimate-context">—</b></div><small>Deterministic synthetic teaching estimate · no production claim</small></article></div></section>
  <section class="section method"><p class="eyebrow">04 / CRISP-DM trace</p><div class="method-grid"><div><h2>From future-safe split<br />to useful evidence.</h2><p>The experiment cleans structural defects, derives distance/time features, fits a regularized linear model on log duration, and scores a complete chronological holdout.</p></div><ol><li><b>01</b> Validate rows and timestamps</li><li><b>02</b> Split whole pickup-time groups</li><li><b>03</b> Fit on training only</li><li><b>04</b> Compare against baselines</li></ol></div></section></main><footer>PROJECT 01 · NYC TAXI TRIP DURATION <span id="footer-status">artifact-backed API client</span></footer>`;
}

function renderMetrics() {
  const m = state.metrics;
  const model = m.linear_log_target || {};
  const baseline = m.baseline || {};
  const improvement = baseline.mae_seconds ? 1 - model.mae_seconds / baseline.mae_seconds : 0;
  document.querySelector("#hero-mae").textContent = sec(model.mae_seconds);
  document.querySelector("#hero-delta").textContent = `${pct(improvement)} below baseline`;
  document.querySelector("#metrics").innerHTML = html`<article class="metric accent"><small>MODEL MAE</small><strong>${sec(model.mae_seconds)}</strong><span>average absolute error</span></article><article class="metric"><small>BASELINE MAE</small><strong>${sec(baseline.mae_seconds)}</strong><span>global median comparator</span></article><article class="metric"><small>MODEL R²</small><strong>${Number(model.r2).toFixed(3)}</strong><span>variance explained</span></article><article class="metric"><small>HOLDOUT</small><strong>${Number(m.test_rows).toLocaleString()}</strong><span>eligible future rows</span></article>`;
  const split = m.split_cutoff || {};
  document.querySelector("#split").innerHTML = html`<strong>${Number(m.train_rows).toLocaleString()} / ${Number(m.test_rows).toLocaleString()}</strong><span>train / holdout rows</span><code>${esc(split.train_max_pickup_datetime)}<br />→ ${esc(split.test_min_pickup_datetime)}</code><small>Strict forward invariant: train max &lt; test min</small>`;
  document.querySelector("#footer-status").textContent = `source · ${m.source || "checked-in artifacts"}`;
}

function renderImportance() {
  const rows = state.importance.slice(0, 6); const max = Math.max(...rows.map((row) => Number(row.absolute_coefficient) || 0), 1);
  document.querySelector("#importance").innerHTML = rows.map((row) => html`<div class="bar-row"><span>${esc(row.feature)}</span><i><b style="width:${Math.max(4, Number(row.absolute_coefficient) / max * 100)}%"></b></i><strong>${Number(row.absolute_coefficient).toFixed(2)}</strong></div>`).join("");
}

function renderSlice() {
  const result = state.slice; const m = result.metrics; const model = m || {}; const baseline = model.baseline || {};
  document.querySelector("#slice-result").innerHTML = html`<article class="panel slice-card"><div class="panel-head"><div><span class="eyebrow">server-computed slice</span><h3>${esc(result.slice)} · ${esc(result.population)}</h3></div><span class="chip">${Number(model.rows).toLocaleString()} rows</span></div><div class="compare"><div><small>MODEL MAE</small><strong>${sec(model.mae_seconds)}</strong></div><div><small>BASELINE MAE</small><strong>${sec(baseline.mae_seconds)}</strong></div><div><small>R²</small><strong>${model.r2 == null ? "—" : Number(model.r2).toFixed(3)}</strong></div></div><div class="residuals">${result.rows.slice(0, 48).map((row) => html`<i title="${esc(row.pickup_datetime)} · residual ${esc(row.residual_seconds)} sec" style="height:${Math.min(100, Math.max(6, Math.abs(Number(row.residual_seconds)) / 3))}%"></i>`).join("")}</div><p class="note">Distance boundary: ${Number(result.distance_boundary_miles).toFixed(2)} mi. Bars show absolute residual magnitude for the first 48 returned rows; hover for timestamp and signed residual.</p></article><article class="panel"><div class="panel-head"><span class="eyebrow">row evidence</span><span class="chip">predictions.csv</span></div><div class="table"><div class="tr th"><span>pickup</span><span>actual</span><span>prediction</span><span>residual</span></div>${result.rows.slice(0, 8).map((row) => html`<div class="tr"><span>${esc(row.pickup_datetime)}</span><span>${sec(row.actual)}</span><span>${sec(row.prediction)}</span><span class="${Number(row.residual_seconds) > 0 ? "positive" : "negative"}">${Number(row.residual_seconds).toFixed(1)}s</span></div>`).join("")}</div></article>`;
}

async function getJSON(path, options) { const response = await fetch(`${API}${path}`, options); if (!response.ok) throw new Error((await response.json().catch(() => ({}))).detail || `request_failed:${response.status}`); return response.json(); }
async function load() { try { const [metrics, importance] = await Promise.all([getJSON("/experiment"), getJSON("/feature-importance")]); state.metrics = metrics; state.importance = importance; renderMetrics(); renderImportance(); document.querySelector("#health").innerHTML = "<i></i> API ONLINE"; await loadSlice(); } catch (error) { state.error = error; document.querySelector("#health").innerHTML = "<i class=bad></i> API UNAVAILABLE"; document.querySelector("#footer-status").textContent = error.message; document.querySelector("#slice-result").innerHTML = `<div class="error-box">${esc(error.message)}. Start FastAPI on port 8001, then reload.</div>`; } }
async function loadSlice() { state.slice = await getJSON(`/predictions?slice=${encodeURIComponent(state.selectedSlice)}&population=${encodeURIComponent(state.population)}`); renderSlice(); }
async function submitEstimate(event) { event.preventDefault(); const form = new FormData(event.currentTarget); const payload = Object.fromEntries(form.entries()); payload.passenger_count = Number(payload.passenger_count); const error = document.querySelector("#estimate-error"); error.textContent = ""; try { const result = await getJSON("/estimate", {method: "POST", headers: {"content-type": "application/json"}, body: JSON.stringify(payload)}); const seconds = Number(result.estimated_duration_seconds); document.querySelector("#estimate-time").textContent = `${Math.floor(seconds / 60)}:${String(seconds % 60).padStart(2, "0")}`; document.querySelector("#estimate-distance").textContent = `${result.distance_miles} mi`; document.querySelector("#estimate-context").textContent = result.is_rush_hour ? "rush hour" : "off-peak"; document.querySelector("#estimate-summary").textContent = result.disclaimer; } catch (requestError) { error.textContent = requestError.message; } }

shell();
document.querySelector("#population").addEventListener("change", async (event) => { state.population = event.target.value; await loadSlice(); });
document.querySelector("#slice").addEventListener("change", async (event) => { state.selectedSlice = event.target.value; await loadSlice(); });
document.querySelector("#estimate-form").addEventListener("submit", submitEstimate);
load();
